apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glpi-app.fullname" . }}-test-connection"
  labels:
    {{- include "glpi-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glpi-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
