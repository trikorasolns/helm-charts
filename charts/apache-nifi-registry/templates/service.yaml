apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-nifi-registry.fullname" . }}
  labels:
    {{- include "apache-nifi-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apache-nifi-registry.selectorLabels" . | nindent 4 }}
